import React from 'react';
import styles from './WaiterOrderId.module.scss';
// import PropTypes from 'prop-types';
const WaiterOrderId = () => (
  <h2 className={styles.component}>WaiterOrderId view</h2>
);
export default WaiterOrderId;
